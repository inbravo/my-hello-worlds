version: 2

models:
  - name: metricflow_time_spine
    description: "MetricFlow time spine — daily date series required for time-based metric aggregation"
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
