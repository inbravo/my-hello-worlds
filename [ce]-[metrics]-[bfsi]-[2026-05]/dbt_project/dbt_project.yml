name: 'bfsi_metrics'
version: '1.0.0'
config-version: 2

profile: 'bfsi_metrics'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  bfsi_metrics:
    +materialized: table
